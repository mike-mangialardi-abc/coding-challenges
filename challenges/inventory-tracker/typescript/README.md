# Inventory Tracker — TypeScript

## Prerequisites

- Node.js 20+
- npm

## Install dependencies and run tests

```bash
npm install
npm test
```

You should initially see **10 failing tests**. Your goal is to make them all pass by implementing `InventoryTracker` in `src/inventoryTracker.ts`.

## Files

- `src/inventoryTracker.ts` — the class you need to implement.
- `src/inventoryTracker.test.ts` — the test suite. Do not modify.
- `package.json`, `tsconfig.json` — project config. Do not modify.

## Reminder

Read the **problem statement** at `../README.md` first.
